spring:
  application:
    name: rabbitmq-consumer
  rabbitmq:
    username: guest
    password: guest
    port: 5672
    host: localhost
    publisher-confirms: true
    virtual-host: /

server:
  port: 8003
